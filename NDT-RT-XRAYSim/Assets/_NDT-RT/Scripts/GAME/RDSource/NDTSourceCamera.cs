using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NDTSourceCamera : MonoBehaviour
{
    [SerializeField] private Transform pivotSource;

    private void Update()
    {
        //transform.LookAt(pivotSource);
    }
}